前30页
/**
 * 统一API响应结果封装
 */
public class Result<T> {
    private int code;
    private String message;
    private T data;
    public Result setCode(ResultCode resultCode) {
        this.code = resultCode.code();
        return this;
    }

    public Result setCode(int code) {
        this.code = code;
        return this;
    }

    public int getCode() {
        return code;
    }

    public String getMessage() {
        return message;
    }

    public Result setMessage(String message) {
        this.message = message;
        return this;
    }

    public T getData() {
        return data;
    }

    public Result setData(T data) {
        this.data = data;
        return this;
    }

    @Override
    public String toString() {
        return JSON.toJSONString(this);
    }
}
/**
 * 响应码枚举，参考HTTP状态码的语义
 */
public enum ResultCode {
    SUCCESS(200),//成功
    FAIL(400),//失败
    UNAUTHORIZED(401),//未认证（签名错误）
    NOT_FOUND(404),//接口不存在
    INTERNAL_SERVER_ERROR(500);//服务器内部错误


    private final int code;

    ResultCode(int code) {
        this.code = code;
    }

    public int code() {
        return code;
    }
}

/**
 * 全局异常处理
 */
@ControllerAdvice
@ResponseBody
public class GlobalDefaultExceptionHandler {

	/**
	 * 处理 AccessException 类型的异常
	 * @param e
	 * @return
	 */
	@ExceptionHandler(AccessException.class)
	public Result accessExceptionHandler(Exception e) {
		e.printStackTrace();
		return ResultGenerator.genFailCodeResult(ProjectConstant.ACCESS_INVALID,e.getMessage());
	}

	/**
	 * 处理 Exception 类型的异常
	 * @param e
	 * @return
	 */
	@ExceptionHandler(Exception.class)
	public Result defaultExceptionHandler(Exception e) {
		e.printStackTrace();
		return ResultGenerator.genFailCodeResult(ProjectConstant.SERVICE_INVALID,e.getMessage());
	}

	/**
	 * 处理 Service RuntimeException 类型的异常
	 */
	@ExceptionHandler(RuntimeException.class)
	public Result defaultServiceExceptionHandler(Exception e){
		e.printStackTrace();
		return ResultGenerator.genFailCodeResult(ProjectConstant.SERVICE_INVALID,e.getMessage());
	}
}

/**
* 权限异常工具类
**/
public class AccessException extends RuntimeException {
	private static final long serialVersionUID = 1L;

	public AccessException() {
    }

    public AccessException(String message) {
        super(message);
    }

    public AccessException(String message, Throwable cause) {
        super(message, cause);
    }

    public AccessException(Throwable cause) {
        super(cause);
    }

    public AccessException(String message, Throwable cause, boolean enableSuppression, boolean writableStackTrace) {
        super(message, cause, enableSuppression, writableStackTrace);
    }
}

/**
* excel转pdf
*/
public class ExcelToPdfUtil{
    public static FileDomainVO excelToPdf(FileDomainVO  vo) throws Exception{
        //默认失败
        vo.setStatus(ProjectConstant.FailStatus);
        if (!getLicense()) { // 验证License 若不验证则转化出的pdf文档会有水印产生
            System.out.println("验证失败,会产生水印");
        }

        //创建一个doc对象
        Workbook wb =null;
        //输出流
        FileOutputStream os =null;
        ImageSaveOptions iso = null;
        try {
            //统计时间
            long old = System.currentTimeMillis();
            //初始化定义
            File file =null;
            //判断是否转换图片
            if(vo.getDetails().equals(ProjectConstant.DocToJPEG)){
                iso = new ImageSaveOptions(SaveFormat.JPEG);
            }else{
                //file.mkdirs();
                //新建一个空白文档
                file = new File(vo.getOutputfile());//pdf保存路径
                //创建文件夹
                file.mkdirs();
                file = new File(vo.getOutputfile()+vo.getFileNameAfter());//转换成pdf的路径+文件名
                os = new FileOutputStream(file);
            }
            //getInputfile是将要被转化文档
                wb = new Workbook(vo.getInputfile());// 原始excel路径
            if(vo.getDetails().equals(ProjectConstant.ExcelToPDF)){
                wb.save(os, com.aspose.cells.SaveFormat.PDF);
            }
            //------------------------------------------逻辑判断
            //统计时间
            long now = System.currentTimeMillis();
            //System.out.println("共耗时：" + ((now - old) / 1000.0) + "秒"+"文件保存在:" + vo.getOutputfile());
            //设置时间
            vo.setTimeConsuming((((now - old) / 1000.0)+"").trim());
            //成功
            vo.setStatus(ProjectConstant.SuccessStatus);
        } catch (Exception e) {
            e.printStackTrace();
        }
        return vo;
    }

    /**
     * 验证License (excel和word引用的License对象不一样，要注意)
     * @return boolean
     */
    public static boolean getLicense() throws Exception{
        boolean result = false;
        InputStream is = null;
        try {
            //File file = new File("src/main/resources/static/license.xml");
            File file = new File("C:\\license.xml");
            is = new FileInputStream(file);
            License aposeLic = new License();
            aposeLic.setLicense(is);
            result = true;
        } catch (Exception e) {
            e.printStackTrace();
        }
        return result;
    }
}

/**
* Excel导出工具类
**/
@SuppressWarnings("deprecation")
public class ExportExcel {

    /**
     * 名称
     */
    private String   name;

    /**
     * 构造方法
     */
    public ExportExcel() {

    }

    /** 
     * EXCEL名称 
     * @param name 
     */
public ExportExcel(String name) 
{

        this.name = name;
    }

    /** 
     * 生成EXCEL 
     * @param response HttpServletResponse
     * @param data 数据 
     * @param headers 表头 
     * @throws Exception 异常
     */
    public void exportExcel(HttpServletResponse response, List<Map<String, Object>> data, Columns[] headers)
            throws Exception 
{
        this.exportExcel(response, data, headers, null);
    }

    public void exportExcelPlus(HttpServletResponse response, List<Map<String, Object>> data, List<ExportExcel.Columns> columnsList)
            throws Exception 
{
        this.exportExcelPlus(response, data, columnsList, null);
    }

    /** 
     * 生成EXCEL 
     * @param response HttpServletResponse
     * @param data 数据 
     * @param headers 表头 
     * @param footers 尾行 
     * @return 返回相对路径，失败返回null 
     * @throws Exception 异常
     */
    @SuppressWarnings({ "rawtypes", "unchecked" })
    public HSSFWorkbook exportExcel(HttpServletResponse response, List<Map<String, Object>> data, Columns[] headers,
            List<Map> footers) throws Exception
 {
        HSSFWorkbook workbook = new HSSFWorkbook();
        HSSFSheet sheet = workbook.createSheet(name);
        //四个参数分别是：起始行，起始列，结束行，结束列      
        // sheet.addMergedRegion(new Region(0, (short) (celln + 1), 0,(short) (celle + 1)));      

        //生成表头单元样式       
        HSSFCellStyle headerStyle = workbook.createCellStyle();
        headerStyle.setFillForegroundColor(HSSFColor.SKY_BLUE.index);
        headerStyle.setFillPattern(HSSFCellStyle.SOLID_FOREGROUND);
        headerStyle.setBorderBottom(HSSFCellStyle.BORDER_THIN);
        headerStyle.setBorderLeft(HSSFCellStyle.BORDER_THIN);
        headerStyle.setBorderRight(HSSFCellStyle.BORDER_THIN);
        headerStyle.setBorderTop(HSSFCellStyle.BORDER_THIN);
        headerStyle.setAlignment(HSSFCellStyle.ALIGN_CENTER);
        //生成一个字体         
        HSSFFont headerFont = workbook.createFont();
        headerFont.setColor(HSSFColor.WHITE.index);
        headerFont.setFontHeightInPoints((short) 12);
        headerFont.setBoldweight(HSSFFont.BOLDWEIGHT_BOLD);
        headerStyle.setFont(headerFont);

        //生成数据单元样式      
        HSSFCellStyle dataStyle = workbook.createCellStyle();
        dataStyle.setFillForegroundColor(HSSFColor.WHITE.index);
        dataStyle.setFillPattern(HSSFCellStyle.SOLID_FOREGROUND);
        dataStyle.setBorderBottom(HSSFCellStyle.BORDER_THIN);
        dataStyle.setBorderLeft(HSSFCellStyle.BORDER_THIN);
        dataStyle.setBorderRight(HSSFCellStyle.BORDER_THIN);
        dataStyle.setBorderTop(HSSFCellStyle.BORDER_THIN);
        dataStyle.setAlignment(HSSFCellStyle.ALIGN_CENTER);
        dataStyle.setVerticalAlignment(HSSFCellStyle.VERTICAL_CENTER);
        //生成另一个字体         
        HSSFFont dataFont = workbook.createFont();
        dataFont.setBoldweight(HSSFFont.BOLDWEIGHT_NORMAL);
        //把字体应用到当前的样式         dataStyle.setFont(dataFont); 

        //尾行样式         
        HSSFCellStyle footerStyle = workbook.createCellStyle();
        footerStyle.setFillPattern(HSSFCellStyle.SOLID_FOREGROUND);
        footerStyle.setBorderBottom(HSSFCellStyle.BORDER_THIN);
        footerStyle.setBorderLeft(HSSFCellStyle.BORDER_THIN);
        footerStyle.setBorderRight(HSSFCellStyle.BORDER_THIN);
        footerStyle.setBorderTop(HSSFCellStyle.BORDER_THIN);
        footerStyle.setAlignment(HSSFCellStyle.ALIGN_CENTER);
        footerStyle.setVerticalAlignment(HSSFCellStyle.VERTICAL_CENTER);
        //生成一个字体        
        HSSFFont footerFont = workbook.createFont();
        footerFont.setBoldweight(HSSFFont.BOLDWEIGHT_BOLD);
        footerStyle.setFont(footerFont);

        //生成表头  
        HSSFRow row = sheet.createRow(0);
        Map<String, Integer> columnIndex = new HashMap<String, Integer>();
        for (int i = 0; i < headers.length; i++) {
            //设置列宽           
            sheet.setColumnWidth((short) i, (short) (headers[i].getWidth() * 256));
            //填充数据          
            HSSFCell cell = row.createCell((short) i);
            cell.setCellStyle(headerStyle);
            HSSFRichTextString text = new HSSFRichTextString(headers[i].getTitle());
            cell.setCellValue(text);

            columnIndex.put(headers[i].getField(), i);
        }
        //生成数据        
        for (int i = 0; i < data.size(); i++) {
            int excelIndex = i + 1; //实际EXCEL行号            
            HSSFRow dataRow = sheet.createRow((short) excelIndex);
            Map map = data.get(i);
            
            for (int h = 0; h < headers.length; h++) {
                HSSFCell cell = dataRow.createCell((short) h);
                cell.setCellStyle(dataStyle);
                Object cellValue = headers[h].getFormatter().formatter(map.get(headers[h].getField()), excelIndex);
                dataProcessing(cell, cellValue);
            }
        }
        //设置尾行       
        if (footers != null) {
            int footersIndex = data.size() + 1;
            for (int i = 0; i < footers.size(); i++) {
                footersIndex += i;
                HSSFRow footerRow = sheet.createRow((short) footersIndex);
                Map<String, Object> map = footers.get(i);
                for (Map.Entry<String, Object> m : map.entrySet()) {
                    HSSFCell cell = footerRow.createCell(columnIndex.get(m.getKey()).shortValue());
                    cell.setCellStyle(footerStyle);
                    dataProcessing(cell, m.getValue());
                }
            }
        }
        // 设置response参数，打开下载页面
        response.reset();
        response.setContentType("application/vnd.ms-excel;charset=utf-8");
        OutputStream out = null;
        try {
            // 设置返回文件名
            response.setHeader("Content-Disposition",
                    "attachment;filename=" + new String((name + ".xls").getBytes("GBK"), "ISO8859-1"));
            // 获取页面返回输出流
            out = response.getOutputStream();
            // Excel文件写入输出流
            workbook.write(out);
        } catch (Exception e) {
            throw e;
        } finally {
            FileUtil.closeAll(out);
        }
        return workbook;

    }

    /** 
     * 对各种类型处理 
     * @param value 值
     * @param cell 列
     */
    private void dataProcessing(HSSFCell cell, Object value) {

        if (value instanceof Date) {
            cell.setCellValue((Date) value);
        } else if (value instanceof Boolean) {
            cell.setCellValue((Boolean) value);
        } else if (value instanceof Double) {
            cell.setCellValue((Double) value);
        } else if (value instanceof Float || value instanceof Long || value instanceof Integer) {
            cell.setCellValue(Double.valueOf(value.toString()));
        } else {
            HSSFRichTextString str = new HSSFRichTextString(StringUtils.commCovent(value));
            cell.setCellValue(str);
        }
    }

    /** 
     * 数据处理回调 
     */
    public interface Formatter {

        /** 
         * 数据单元的回调方法 
         * @param value 
         * @param index   
         * @return 返回处理后的数据 
         */
        public Object formatter(Object value, int index);
    }

    /** 
     * 表头数据类型 
     */
    public class Columns {

        /**
         * 域
         */
        private String field;

        /**
         * 标题
         */
        private String title;

        /**
         * 宽度
         */
        private int width = 15;

        /**
         * 格式
         */
        private Formatter formatter = new Formatter() {

            public Object formatter(Object value, int index) {

                return value;
            }
        };

        /** 
         * 设置数据 
         * @param field 字段 
         * @param title 标题   
         */
        public Columns(String field, String title) {

            this.field = field;
            this.title = title;
        }

        /**
         * 域获得
         * @return 域
         */
        public String getField() {

            return field;
        }

        /**
         * 标题获得
         * @return 标题
         */
        public String getTitle() {

            return title;
        }

        /**
         * 格式获得
         * @return 格式
         */
        public Formatter getFormatter() {

            return formatter;
        }

        /** 
         * 数据处理回调方法 
         * @param formatter  Formatter
         * @return 列信息
         */
        public Columns setFormatter(Formatter formatter) {

            this.formatter = formatter;
            return this;
        }

        /**
         * 宽度获得
         * @return 宽度
         */
        public int getWidth() {

            return width;
        }

        /** 
         * 设置表格宽度（字符为单位） 
         * @param width 宽
         * @return 列信息
         */
        public Columns setWidth(int width) {

            this.width = width;
            return this;
        }
    }

    /**
     * 生成EXCEL加强版
     * @param response HttpServletResponse
     * @param data 数据
     * @param columnsList 表头
     * @param footers 尾行
     * @return 返回相对路径，失败返回null
     * @throws Exception 异常
     */
    @SuppressWarnings({ "rawtypes", "unchecked" })
    public HSSFWorkbook exportExcelPlus(HttpServletResponse response, List<Map<String, Object>> data, List<ExportExcel.Columns> columnsList,
                                    List<Map> footers) throws Exception {
        HSSFWorkbook workbook = new HSSFWorkbook();
        HSSFSheet sheet = workbook.createSheet(name);
        //四个参数分别是：起始行，起始列，结束行，结束列
        // sheet.addMergedRegion(new Region(0, (short) (celln + 1), 0,(short) (celle + 1)));

        //生成表头单元样式
        HSSFCellStyle headerStyle = workbook.createCellStyle();
        headerStyle.setFillForegroundColor(HSSFColor.SKY_BLUE.index);
        //自定义设置颜色
        HSSFPalette palette = workbook.getCustomPalette();
        palette.setColorAtIndex(HSSFColor.SKY_BLUE.index, (byte) 242, (byte) 242, (byte) 242);
        headerStyle.setFillPattern(HSSFCellStyle.SOLID_FOREGROUND);
        headerStyle.setBorderBottom(HSSFCellStyle.BORDER_THIN);
        headerStyle.setBorderLeft(HSSFCellStyle.BORDER_THIN);
        headerStyle.setBorderRight(HSSFCellStyle.BORDER_THIN);
        headerStyle.setBorderTop(HSSFCellStyle.BORDER_THIN);
        headerStyle.setAlignment(HSSFCellStyle.ALIGN_CENTER);
        //生成一个字体
        HSSFFont headerFont = workbook.createFont();
        headerFont.setColor(HSSFColor.BLACK.index);
        headerFont.setFontHeightInPoints((short) 12);
        //加粗显示
        headerFont.setBoldweight(HSSFFont.BOLDWEIGHT_BOLD);
        headerStyle.setFont(headerFont);

        //生成数据单元样式
        HSSFCellStyle dataStyle = workbook.createCellStyle();
        dataStyle.setFillForegroundColor(HSSFColor.WHITE.index);
        dataStyle.setFillPattern(HSSFCellStyle.SOLID_FOREGROUND);
        dataStyle.setBorderBottom(HSSFCellStyle.BORDER_THIN);
        dataStyle.setBorderLeft(HSSFCellStyle.BORDER_THIN);
        dataStyle.setBorderRight(HSSFCellStyle.BORDER_THIN);
        dataStyle.setBorderTop(HSSFCellStyle.BORDER_THIN);
        dataStyle.setAlignment(HSSFCellStyle.ALIGN_CENTER);
        dataStyle.setVerticalAlignment(HSSFCellStyle.VERTICAL_CENTER);
        //生成另一个字体
        HSSFFont dataFont = workbook.createFont();
        dataFont.setBoldweight(HSSFFont.BOLDWEIGHT_NORMAL);
        //把字体应用到当前的样式         dataStyle.setFont(dataFont);

        //尾行样式
        HSSFCellStyle footerStyle = workbook.createCellStyle();
        footerStyle.setFillPattern(HSSFCellStyle.SOLID_FOREGROUND);
        footerStyle.setBorderBottom(HSSFCellStyle.BORDER_THIN);
        footerStyle.setBorderLeft(HSSFCellStyle.BORDER_THIN);
        footerStyle.setBorderRight(HSSFCellStyle.BORDER_THIN);
        footerStyle.setBorderTop(HSSFCellStyle.BORDER_THIN);
        footerStyle.setAlignment(HSSFCellStyle.ALIGN_CENTER);
        footerStyle.setVerticalAlignment(HSSFCellStyle.VERTICAL_CENTER);
        //生成一个字体
        HSSFFont footerFont = workbook.createFont();
        footerFont.setBoldweight(HSSFFont.BOLDWEIGHT_BOLD);
        footerStyle.setFont(footerFont);

        //生成表头
        HSSFRow row = sheet.createRow(0);
        Map<String, Integer> columnIndex = new HashMap<String, Integer>();

        for(int i = 0;i<columnsList.size();i++){
            //设置列宽
            sheet.setColumnWidth((short) i, (short) (columnsList.get(i).getWidth() * 256));
            //填充数据
            HSSFCell cell = row.createCell((short) i);
            cell.setCellStyle(headerStyle);
            HSSFRichTextString text = new HSSFRichTextString(columnsList.get(i).getTitle());
            cell.setCellValue(text);

            columnIndex.put(columnsList.get(i).getField(), i);
        }
        //生成数据
        for (int i = 0; i < data.size(); i++) {
            int excelIndex = i + 1; //实际EXCEL行号
            HSSFRow dataRow = sheet.createRow((short) excelIndex);
            Map map = data.get(i);

            for (int h = 0; h < columnsList.size(); h++) {
                HSSFCell cell = dataRow.createCell((short) h);
                cell.setCellStyle(dataStyle);
                Object cellValue = columnsList.get(h).getFormatter().formatter(map.get(columnsList.get(h).getField()), excelIndex);
                dataProcessing(cell, cellValue);
            }
        }
        //设置尾行
        if (footers != null) {
            int footersIndex = data.size() + 1;
            for (int i = 0; i < footers.size(); i++) {
                footersIndex += i;
                HSSFRow footerRow = sheet.createRow((short) footersIndex);
                Map<String, Object> map = footers.get(i);
                for (Map.Entry<String, Object> m : map.entrySet()) {
                    HSSFCell cell = footerRow.createCell(columnIndex.get(m.getKey()).shortValue());
                    cell.setCellStyle(footerStyle);
                    dataProcessing(cell, m.getValue());
                }
            }
        }
        // 设置response参数，打开下载页面
        response.reset();
        response.setContentType("application/vnd.ms-excel;charset=utf-8");
        //跨域设置
        response.setHeader("Access-Control-Allow-Origin", "*");
        response.setHeader("Cache-Control","no-cache");
        OutputStream out = null;
        try {
            // 设置返回文件名
            response.setHeader("Content-Disposition",
                    "attachment;filename=" + new String((name + ".xls").getBytes("GBK"), "ISO8859-1"));
            // 获取页面返回输出流
            out = response.getOutputStream();
            // Excel文件写入输出流
            workbook.write(out);
        } catch (Exception e) {
            throw e;
        } finally {
            FileUtil.closeAll(out);
        }
        return workbook;
    }

    public static void exportExcelPlus(String exportCondition,List<?> list,HttpServletResponse response){
        SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        List<Map<String, Object>> data = new ArrayList<Map<String, Object>>();
        String name = "表格导出数据";
        try {
            Object obj = null;
            Iterator<?> listIt = list.iterator();
            while(listIt.hasNext()){
                obj = listIt.next();
                Map<String, Object> temp = new HashMap<String, Object>();
                if(!exportCondition.isEmpty()) {
                    JSONArray jsonArray = JSONArray.parseArray(exportCondition);
                    if(jsonArray.size()>0){
                        for(int i=0;i<jsonArray.size();i++){
                            JSONObject jsonObject = jsonArray.getJSONObject(i);
                            String strKey = jsonObject.get("field")+"";//查询Key
                            //获取方法
                            System.out.println("get"+StringUtils.toUpperCaseFirstOne(strKey));
                            Method m = obj.getClass().getDeclaredMethod("get"+StringUtils.toUpperCaseFirstOne(strKey));
                            //调用方法
                            temp.put(strKey, m.invoke(obj) );
                        }
                    }
                }
                data.add(temp);
            }
        }  catch (NoSuchMethodException e) {
            e.printStackTrace();
        } catch (SecurityException e) {
            e.printStackTrace();
        } catch (IllegalAccessException e) {
            e.printStackTrace();
        } catch (IllegalArgumentException e) {
            e.printStackTrace();
        } catch (InvocationTargetException e) {
            e.printStackTrace();
        }

        // 表头列设置
        if(list.size()==0){
            Map<String, Object> temp = new HashMap<String, Object>();
            temp.put("warning", "最近没有数据或者您筛选的数据不存在。");
            data.add(temp);
            HSSFWorkbook workbook = new HSSFWorkbook();
            HSSFSheet sheet = workbook.createSheet(name);
            Columns c = new ExportExcel().new Columns("warning", "提示");
            c.setWidth(50);
            ExportExcel.Columns[] headers = new ExportExcel.Columns[] {
                    c,
            };
            try {
                new ExportExcel(name).exportExcel(response, data, headers);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }else{
            List<ExportExcel.Columns> columnsList = new ArrayList<>();
            if(!exportCondition.isEmpty()) {
                JSONArray jsonArray = JSONArray.parseArray(exportCondition);
                if(jsonArray.size()>0){
                    for(int i=0;i<jsonArray.size();i++){
                        JSONObject jsonObject = jsonArray.getJSONObject(i);
                        String strKey = jsonObject.get("field")+"";//查询Key
                        String strValue = jsonObject.get("value")+"";//查询条件值
                        Columns c = new ExportExcel().new Columns(strKey, strValue);
                        columnsList.add(c);
                    }
                }
            }
            try {
                new ExportExcel(name).exportExcelPlus(response, data, columnsList);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }
}

/**
 * Word导出工具类
 **/
public class ExportWord {
    /**
     * 导出word
     * <p>第一步生成替换后的word文件，只支持docx</p>
     * <p>第二步下载生成的文件</p>
     * <p>第三步删除生成的临时文件</p>
     * 模版变量中变量格式：{{foo}}
     * @param templatePath word模板地址
     * @param temDir 生成临时文件存放地址
     * @param params 替换的参数
     * @param request HttpServletRequest
     * @param response HttpServletResponse
     */
    public static void exportWord(String templatePath, String temDir, Map<String, Object> params, HttpServletRequest request, HttpServletResponse response) {
        Assert.notNull(templatePath,"模板路径不能为空");
        Assert.notNull(temDir,"临时文件路径不能为空");
        String fileName = DateUtil.getDate() + UUID.randomUUID() + ProjectConstant.WORD_DOCX;
        if (!temDir.endsWith("/")){
            temDir = temDir + File.separator;
        }
        File dir = new File(temDir);
        if (!dir.exists()) {
            dir.mkdirs();
        }
        // 设置response参数，打开下载页面
        response.reset();
        response.setContentType("application/msword;charset=utf-8");
        //跨域设置
        response.setHeader("Access-Control-Allow-Origin", "*");
        response.setHeader("Cache-Control","no-cache");
        try {
            XWPFDocument doc = WordExportUtil.exportWord07(templatePath, params);
            String tmpPath = temDir + fileName;
            FileOutputStream fos = new FileOutputStream(tmpPath);
            doc.write(fos);
            // 设置强制下载不打开
            response.setContentType("application/force-download");
            // 设置文件名
            response.addHeader("Content-Disposition", "attachment;fileName=" + fileName);
            OutputStream out = response.getOutputStream();
            doc.write(out);
            out.close();
        } catch (Exception e) {
            e.printStackTrace();
        } finally {
            File file = new File(temDir+fileName);
            FileUtil.deleteFile(file);//这一步看具体需求，要不要删除零时文件夹下的文件
        }
    }
}

/**
 *文件操作类
 **/
public class FileUtil {
    public static void uploadFile(byte[] file, String filePath, String fileName) throws Exception {
        File targetFile = new File(filePath);
        if (!targetFile.exists()) {
            targetFile.mkdirs();
        }
        FileOutputStream out = new FileOutputStream(filePath + fileName);
        out.write(file);
        out.flush();
        out.close();
    }


    public static String getExtensionName(String filename) {
        if ((filename != null) && (filename.length() > 0)) {
            int dot = filename.lastIndexOf('.');
            if ((dot > -1) && (dot < (filename.length() - 1))) {
                return filename.substring(dot + 1);
            }
        }
        return filename;
    }

    //Java文件操作 获取不带扩展名的文件名
    public static String getFileNameNoEx(String filename) {
        if ((filename != null) && (filename.length() > 0)) {
            int dot = filename.lastIndexOf('.');
            if ((dot > -1) && (dot < (filename.length()))) {
                return filename.substring(0, dot);
            }
        }
        return filename;
    }
    /**
     * 下载项目根目录下doc下的文件
     * @param response response
     * @param fileName 文件名
     * @return 返回结果 成功或者文件不存在
     */
    public static String downloadFile(HttpServletResponse response, String fileName, boolean notIE) {
        InputStream stream = FileUtil.class.getClassLoader().getResourceAsStream("UploadFiles/" + fileName);
        response.setHeader("content-type", "application/octet-stream");
        response.setContentType("application/octet-stream");
        try {
            String name = java.net.URLEncoder.encode(fileName, "UTF-8");
            if (notIE) {
                name = java.net.URLDecoder.decode(name, "ISO-8859-1");
            }
            response.setHeader("Content-Disposition", "attachment;filename=" + name );
        } catch (UnsupportedEncodingException e2) {
            e2.printStackTrace();
        }
        byte[] buff = new byte[1024];
        BufferedInputStream bis = null;
        OutputStream os = null;
        try {
            os = response.getOutputStream();
            bis = new BufferedInputStream(stream);
            int i = bis.read(buff);
            while (i != -1) {
                os.write(buff, 0, buff.length);
                os.flush();
                i = bis.read(buff);
            }
        } catch (FileNotFoundException e1) {
            //e1.getMessage()+"系统找不到指定的文件";
            return "系统找不到指定的文件";
        }catch (IOException e) {
            e.printStackTrace();
        } finally {
            if (bis != null) {
                try {
                    bis.close();
                } catch (IOException e) {
                    e.printStackTrace();
                }
            }
        }
        return "success";
    }

    //删除文件和目录
    public static void clearFiles(String path){
        File file = new File(path);
        if(file.exists()){
            deleteFile(file);
        }
    }

    //删除文件
    public static void deleteFile(File file){
        if(file.isDirectory()){
            File[] files = file.listFiles();
            for(int i=0; i<files.length; i++){
                deleteFile(files[i]);
            }
        }
        file.delete();
    }

    /**
     * @Description:  做一个关闭流接口的形参，里面的参数代表可变参数，
     * 不管传进来多少个参数都会放到数组里面,
     * @param
     * @return
     * @throws
     * @author WKF
     * @UpdateUser:
     * @UpdateDate:   2019/1/23 14:57
     * @UpdateRemark:   修改内容
     * @date  2019/1/23 14:57
     */
    public static void closeAll(Closeable... cs) {
        for (Closeable c : cs) {// 遍历数组
            if (cs != null) {// 只要传进来的不是空的都给它把流关闭
                try {
                    c.close();
                } catch (IOException e) {
                    e.printStackTrace();
                }
            }
        }
    }
}
/**
* 系统操作日志XML文件
*/
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anshare.project.dao.LogMapper">
  <resultMap id="BaseResultMap" type="com.anshare.project.model.Log">
    <id column="ID" jdbcType="CHAR" property="id" />
    <result column="UserID" jdbcType="VARCHAR" property="userid" />
    <result column="OptTime" jdbcType="VARCHAR" property="opttime" />
    <result column="IP" jdbcType="LONGVARCHAR" property="ip" />
    <result column="Region" jdbcType="LONGVARCHAR" property="region" />
    <result column="Account" jdbcType="LONGVARCHAR" property="account" />
    <result column="OptType" jdbcType="LONGVARCHAR" property="opttype" />
    <result column="Content" jdbcType="LONGVARCHAR" property="content" />
    <result column="ResultContent" jdbcType="LONGVARCHAR" property="resultcontent" />
    <result column="IsDeleted" jdbcType="BIT" property="isdeleted" />
    <result column="Timestamp" jdbcType="TIMESTAMP" property="timestamp" />
  </resultMap>
</mapper>

/**
 * Service 层 基础接口，其他Service 接口 请继承该接口
 */
public interface Service<T> {
    // 保存  ps:
    // if (Guid=="") 则会自动生成一个Guid
    // else 则以传入的Guid为准
    void save(T model,boolean setGuid);

    void save(List<T> models);//batch批量保存

    void deleteById(String id);//通过主鍵刪除 (软删除，更新IsDeleted=1)

    void deleteByIds(String ids);//批量刪除 eg：ids -> “1,2,3,4”

    void deleteByCondition(Condition con);//真实删除(通过条件删除);

    void update(T model);//更新

    void updateByNull(T model);//更新(NULL值会覆盖原有值)

    T findById(String id);//通过ID查找

    T findBy(String fieldName, Object value) throws TooManyResultsException; //通过Model中某个成员变量名称（非数据表中column的名称）查找,value需符合unique约束

    List<T> findByIds(String ids);//通过多个ID查找//eg：ids -> “1,2,3,4”

    List<T> findByCondition(Condition condition);//根据条件查找

    List<T> findAll();//获取所有

    int selectCount(T var1);

    //List<T> list(String sql);//获取所有，包含分页

    List<T> resumeList(String id);//获取简历信息

    List<T> findListByIds(String ids);//通过多个ID查找//eg：ids -> “1,2,3,4”

}

package com.anshare.project.core;


import com.anshare.project.core.Util.ServiceException;
import com.anshare.project.core.Util.UUIDUtils;
import org.apache.ibatis.exceptions.TooManyResultsException;
import org.springframework.beans.factory.annotation.Autowired;
import tk.mybatis.mapper.entity.Condition;

import java.lang.reflect.Field;
import java.lang.reflect.ParameterizedType;
import java.sql.Timestamp;
import java.util.Date;
import java.util.List;
import java.util.UUID;

/**
 * 基于通用MyBatis Mapper插件的Service接口的实现
 */
public abstract class AbstractService<T> implements Service<T> {

    @Autowired
    protected Mapper<T> mapper;
    private Class<T> modelClass;    // 当前泛型真实类型的Class

    public AbstractService() {
        ParameterizedType pt = (ParameterizedType) this.getClass().getGenericSuperclass();
        modelClass = (Class<T>) pt.getActualTypeArguments()[0];
    }

    public void save(T model,boolean setGuid) {
        Field field = null;
        try {
            if(setGuid)
            {
                field = modelClass.getDeclaredField("id");
                field.setAccessible(true);
                String mid = UUID.randomUUID().toString();

                field.set(model,mid);
            }
            Field field2 = modelClass.getDeclaredField("timestamp");
            field2.setAccessible(true);
            Timestamp ts=new Timestamp(new Date().getTime());
            field2.set(model, ts);

            Field field3 = modelClass.getDeclaredField("isdeleted");
            field3.setAccessible(true);
            field3.set(model, false);

        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        mapper.insertSelective(model);
    }

    public void save(List<T> models) {
        for(T model:models){
            Field field = null;
            try {
                field = modelClass.getDeclaredField("id");
                field.setAccessible(true);
                String mid = UUIDUtils.getUUID();
                field.set(model, mid);

                Field field2 = modelClass.getDeclaredField("timestamp");
                field2.setAccessible(true);
                Timestamp ts=new Timestamp(new Date().getTime());
                field2.set(model, ts);

                Field field3 = modelClass.getDeclaredField("isdeleted");
                field3.setAccessible(true);
                field3.set(model, false);
            } catch (ReflectiveOperationException e) {
                throw new ServiceException(e.getMessage(), e);
            }
        }
        mapper.insertList(models);
    }

    public void deleteById(String id) {
        try {
            T model = findById(id);
            Field field = modelClass.getDeclaredField("isdeleted");
            field.setAccessible(true);
            boolean flag = true;
            field.set(model, flag);
            Field field2 = modelClass.getDeclaredField("timestamp");
            field2.setAccessible(true);
            Timestamp ts=new Timestamp(new Date().getTime());
            field2.set(model, ts);
             mapper.updateByPrimaryKeySelective(model);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        //mapper.deleteByPrimaryKey(id);
    }
	/**
	 * 创建 SSL连接
	 */
	public static CloseableHttpClient createSSLInsecureClient() {
		try 
{
			SSLContext sslContext = new SSLContextBuilder().loadTrustMaterial(new TrustStrategy() 
{
				@Override
				public boolean isTrusted(X509Certificate[] chain, String authType) throws CertificateException {
					return true;
				}
			}).build();
			SSLConnectionSocketFactory sslsf = new SSLConnectionSocketFactory(sslContext, new HostnameVerifier() {
				@Override
				public boolean verify(String hostname, SSLSession session) {
					return true;
				}
			});
			return HttpClients.custom().setSSLSocketFactory(sslsf).build();
		} catch (GeneralSecurityException ex) {
			throw new RuntimeException(ex);
		}
	}
	
	public static String post(String url, String params) throws Exception {
		CloseableHttpClient httpclient = HttpClients.createDefault();
		HttpPost httpPost = new HttpPost(url);
		httpPost.setHeader("Accept", "application/json");
		httpPost.setHeader("Content-Type", "application/json");
		String charSet = "UTF-8";
		StringEntity entity = new StringEntity(params, charSet);
		httpPost.setEntity(entity);
		CloseableHttpResponse response = null;
		try {
			response = httpclient.execute(httpPost);
			StatusLine status = response.getStatusLine();
			int state = status.getStatusCode();
			if (state == HttpStatus.SC_OK) {
				HttpEntity responseEntity = response.getEntity();
				String jsonString = EntityUtils.toString(responseEntity);
				return jsonString;
			} else {
				log.error("请求返回:" + state + "(" + url + ")");
			}
		} finally {
			if (response != null) {
				try {
					response.close();
				} catch (IOException e) {
					e.printStackTrace();
				}
			}
			try {
				httpclient.close();
			} catch (IOException e) {
				e.printStackTrace();
			}
		}
		return null;
	}

	/**
	 * 调用http的通用post方法
	 * @return
	 */
	public static String api4Http(String url,String requestData){
        log.info("api4Http----------------------------");
		//实例化httpclient
		CloseableHttpClient httpclient = HttpClients.createDefault();
		//实例化post方法
		HttpPost httpPost = new HttpPost(url);
		//处理参数
		List<NameValuePair> nvps = new ArrayList<NameValuePair>();
		nvps.add(new BasicNameValuePair("requestData",requestData));
		log.info("咨询建议接口参数={}");
		//请求结果
		CloseableHttpResponse response = null;
		String content ="";
		try {
			//提交的参数
			UrlEncodedFormEntity uefEntity  = new UrlEncodedFormEntity(nvps, "UTF-8");
			//将参数给post方法
			httpPost.setEntity(uefEntity);
			//执行post方法
			response = httpclient.execute(httpPost);
			if(response != null && response.getStatusLine().getStatusCode()==200){
				content = EntityUtils.toString(response.getEntity(),"utf-8");
			}
			log.info("信息========================="+content);
		} catch (ClientProtocolException e) {
		    log.error("咨询建议异常");
		} catch (IOException e) {
            log.error("咨询建议异常");
		}finally{
			if(httpclient != null){
				try {
					httpclient.close();
				} catch (IOException e) {
					e.printStackTrace();
				}
			}
		}
		return content;
	}
	/**
	 * ws连接测试 get请求
	 * @param url
	 * @return
	 */
	public static String api4Get(String url){

		//实例化httpclient
		CloseableHttpClient httpclient = HttpClients.createDefault();
		//实例化get方法
		HttpGet httpget = new HttpGet(url);
		//请求结果
		CloseableHttpResponse response = null;
		String content ="";
		try {
			//执行get方法
			response = httpclient.execute(httpget);
			if(response !=null && response.getStatusLine().getStatusCode()==200){
				content = EntityUtils.toString(response.getEntity(),"utf-8");
			}
		} catch (ClientProtocolException e) {
			e.printStackTrace();
		} catch (IOException e) {
			e.printStackTrace();
		}finally{
			if(httpclient != null){
				try {
					httpclient.close();
				} catch (IOException e) {
					e.printStackTrace();
				}
			}
		}
		return content;
	}
}
/**
* 全面支持DOC, DOCX, OOXML, RTF HTML, OpenDocument, PDF, EPUB, XPS, SWF 相互转换
*/
public class WordToPdfUtil {
    /**
     * word转换
     * 全面支持DOC, DOCX, OOXML, RTF HTML, OpenDocument, PDF, EPUB, XPS, SWF 相互转换
     * @param
     */
    public static FileDomainVO wordToPdf(FileDomainVO  vo) throws Exception{
        //默认失败
        vo.setStatus(ProjectConstant.FailStatus);
        if (!getLicense()) { // 验证License 若不验证则转化出的pdf文档会有水印产生
            System.out.println("验证失败,会产生水印");
        }
 public void deleteByIds(String ids) {
        mapper.deleteByIds(ids);
    }

    public void deleteByCondition(Condition con) {
        mapper.deleteByCondition(con);
    }

    public void update(T model) {
        try {
            Field field2 = modelClass.getDeclaredField("timestamp");
            field2.setAccessible(true);
            Timestamp ts=new Timestamp(new Date().getTime());
            field2.set(model, ts);
            mapper.updateByPrimaryKeySelective(model);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        mapper.updateByPrimaryKeySelective(model);
    }
    public void updateByNull(T model) {
        try {
            Field field2 = modelClass.getDeclaredField("timestamp");
            field2.setAccessible(true);
            Timestamp ts=new Timestamp(new Date().getTime());
            field2.set(model, ts);
            mapper.updateByPrimaryKey(model);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        mapper.updateByPrimaryKey(model);
    }

    public T findById(String id) {
        return mapper.selectByPrimaryKey(id);
    }

    @Override
    public T findBy(String fieldName, Object value) throws TooManyResultsException {
        try {
            T model = modelClass.newInstance();
            Field field = modelClass.getDeclaredField(fieldName);
            field.setAccessible(true);
            field.set(model, value);
            return mapper.selectOne(model);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
    }

    public List<T> findByIds(String ids) {
        return mapper.selectByIds(ids);
    }

    public List<T> findByCondition(Condition condition) {
        return mapper.selectByCondition(condition);
    }

    public List<T> findAll() {
        Condition condition = new Condition(modelClass);
        condition.createCriteria()
                .andEqualTo("isdeleted",false);
        return findByCondition(condition);
    }

    public int selectCount(T var1){
        try {
            Field field3 = modelClass.getDeclaredField("isdeleted");
            field3.setAccessible(true);
            field3.set(var1, false);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        return mapper.selectCount(var1);
}
 public void deleteByIds(String ids) {
        mapper.deleteByIds(ids);
    }

    public void deleteByCondition(Condition con) {
        mapper.deleteByCondition(con);
    }

    public void update(T model) {
        try {
            Field field2 = modelClass.getDeclaredField("timestamp");
            field2.setAccessible(true);
            Timestamp ts=new Timestamp(new Date().getTime());
            field2.set(model, ts);
            mapper.updateByPrimaryKeySelective(model);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        mapper.updateByPrimaryKeySelective(model);
    }
    public void updateByNull(T model) {
        try {
            Field field2 = modelClass.getDeclaredField("timestamp");
            field2.setAccessible(true);
            Timestamp ts=new Timestamp(new Date().getTime());
            field2.set(model, ts);
            mapper.updateByPrimaryKey(model);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        mapper.updateByPrimaryKey(model);
    }

    public T findById(String id) {
        return mapper.selectByPrimaryKey(id);
    }

    @Override
    public T findBy(String fieldName, Object value) throws TooManyResultsException {
        try {
            T model = modelClass.newInstance();
            Field field = modelClass.getDeclaredField(fieldName);
            field.setAccessible(true);
            field.set(model, value);
            return mapper.selectOne(model);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
    }
    public List<T> findByIds(String ids) {
        return mapper.selectByIds(ids);
    }

    public List<T> findByCondition(Condition condition) {
        return mapper.selectByCondition(condition);
    }

    public List<T> findAll() {
        Condition condition = new Condition(modelClass);
        condition.createCriteria()
                .andEqualTo("isdeleted",false);
        return findByCondition(condition);
    }

    public int selectCount(T var1){
        try {
            Field field3 = modelClass.getDeclaredField("isdeleted");
            field3.setAccessible(true);
            field3.set(var1, false);
        } catch (ReflectiveOperationException e) {
            throw new ServiceException(e.getMessage(), e);
        }
        return mapper.selectCount(var1);
    }
        //创建一个doc对象
        Document doc =null;
        //输出流
        FileOutputStream os =null;
        ImageSaveOptions iso = null;
        try {
            //统计时间
            long old = System.currentTimeMillis();
            //初始化定义
            File file =null;
            //判断是否转换图片
            if(vo.getDetails().equals(ProjectConstant.DocToJPEG)){
                iso = new ImageSaveOptions(SaveFormat.JPEG);
            }else{
                //file.mkdirs();
                //新建一个空白文档
                file = new File(vo.getOutputfile());//pdf保存路径
                //创建文件夹
                file.mkdirs();
                file = new File(vo.getOutputfile()+vo.getFileNameAfter());//转换成pdf的路径+文件名
                os = new FileOutputStream(file);
            }
            //getInputfile是将要被转化文档
            doc = new Document(vo.getInputfile());
            //全面支持DOC, DOCX, OOXML, RTF HTML, OpenDocument, PDF, EPUB, XPS, SWF 相互转换
            //------------------------------------------逻辑判断
            //>>>>>>DocToPDF
            if(vo.getDetails().equals(ProjectConstant.DocToPDF)){
                doc.save(os, SaveFormat.PDF);
            }//>>>>>>DocxToPDF
            if(vo.getDetails().equals(ProjectConstant.DocxToPDF)){
                doc.save(os, SaveFormat.PDF);
            }//>>>>>>DocToDocx
            else if(vo.getDetails().equals(ProjectConstant.DocToDocx)){
                doc.save(os, SaveFormat.DOCX);
            }//>>>>>>DocToTexT
            else if(vo.getDetails().equals(ProjectConstant.DocToTexT)){
                doc.save(os, SaveFormat.TEXT);
            }//>>>>>>DocToXps
            else if(vo.getDetails().equals(ProjectConstant.DocToXps)){
                doc.save(os, SaveFormat.XPS);
            }//>>>>>>HtmlToDoc
            else if(vo.getDetails().equals(ProjectConstant.HtmlToDoc)){
                doc.save(os, SaveFormat.DOC);
            }//>>>>>>DocToJPEG
            else if(vo.getDetails().equals(ProjectConstant.DocToJPEG)){
                //iso.setResolution(128);
                iso.setPrettyFormat(true);
                iso.setUseAntiAliasing(true);
                for (int i = 0; i < doc.getPageCount(); i++)
                {
                    iso.setPageIndex(i);
                    doc.save(vo.getOutputfile() + vo.getFileNameAfter() +"---"+(i+1)+ ".jpeg", iso);
                }
            }
            //------------------------------------------逻辑判断
            //统计时间
            long now = System.currentTimeMillis();
            //System.out.println("共耗时：" + ((now - old) / 1000.0) + "秒"+"文件保存在:" + vo.getOutputfile());
            //设置时间
            vo.setTimeConsuming((((now - old) / 1000.0)+"").trim());
            //成功
            vo.setStatus(ProjectConstant.SuccessStatus);
        } catch (Exception e) {
            e.printStackTrace();
        }
        return vo;
    }
    /**
     * 验证License
     * @return boolean
     */
    public static boolean getLicense() throws Exception{
        boolean result = false;
        InputStream is = null;
        try {
            //File file = new File("src/main/resources/static/license.xml");
            File file = new File("C:\\license.xml");
            is = new FileInputStream(file);
            License aposeLic = new License();
            aposeLic.setLicense(is);
            result = true;
        } catch (Exception e) {
            e.printStackTrace();
        }
        return result;
    }
}
/*
* 系统操作日志类
*/
@Data
@Table(name = "log")
public class Log {
    @Id
    @Column(name = "ID")
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private String id;

    /**
     * 操作人
     */
    @Column(name = "UserID")
    private String userid;
    /**
     * 操作时间
     */
    @Column(name = "OptTime")
    private String opttime;
    @Column(name = "IsDeleted")
    private Boolean isdeleted;
    @Column(name = "Timestamp")
    private Date timestamp;
    @Column(name = "IP")
    private String ip;

    /**
     * 操作
     */
    @Column(name = "Region")
    private String region;

    /**
     * 方法
     */
    @Column(name = "Account")
    private String account;

    /**
     * 结果类型（0成功，1失败）
     */
    @Column(name = "OptType")
    private String opttype;

    /**
     * 参数
     */
    @Column(name = "Content")
    private String content;

    /**
     * 参数
     */
    @Column(name = "ResultContent")
    private String resultcontent;
}

/**
 * 系统操作日志LogService
 */
public interface LogService extends Service<Log> {

}

后30页
 /**
 * 定制版MyBatis Mapper插件接口，如需其他接口参考官方文档自行添加。
 */
public interface Mapper<T>
        extends
        BaseMapper<T>,
        ConditionMapper<T>,
        IdsMapper<T>,
        InsertListMapper<T> {

    List<T> resumeList(@Param("id")String id);

    List<T> findListByIds(@Param("ids") String ids);
}
/**
* 自定义注解类
**/
@Target(ElementType.METHOD) //注解放置的目标位置,METHOD是可注解在方法级别上
@Retention(RetentionPolicy.RUNTIME) //注解在哪个阶段执行
@Documented //生成文档
public @interface MyLog {
    String value() default "";
}
if(org.apache.commons.lang3.StringUtils.isEmpty(docApplication.getSearchYear())){
            docApplication.setSearchYear("2018");
        }
        //今年数据
        List<PowerMatters> nowList = docApplicationService.countFormTotal(docApplication);

        docApplication.setProcesstypedict("第一种形态");
        List<PowerMatters> clueDtoOneList = docApplicationService.queryFormsList(docApplication);
        docApplication.setProcesstypedict("第二种形态");
        List<PowerMatters> clueDtoTwoList = docApplicationService.queryFormsList(docApplication);
        docApplication.setProcesstypedict("第三种形态");
        List<PowerMatters> clueDtoThreeList = docApplicationService.queryFormsList(docApplication);
        docApplication.setProcesstypedict("第四种形态");
        List<PowerMatters> clueDtoFourList = docApplicationService.queryFormsList(docApplication);
docApplication.setSearchYear(DateUtil.beforeYear(docApplication.getSearchYear()));
        List<PowerMatters> reduceList = docApplicationService.countFormTotal(docApplication);

        //第一种形态
        ClueDto clueDtoOne = new ClueDto();
        clueDtoOne.setNowTotal(0);
        //第二种形态
        ClueDto clueDtoTwo = new ClueDto();
        clueDtoTwo.setNowTotal(0);
        //第三种形态
        ClueDto clueDtoThree = new ClueDto();
        clueDtoThree.setNowTotal(0);
        //第四种形态
        ClueDto clueDtoFour = new ClueDto();
        clueDtoFour.setNowTotal(0);
        //第一种形态
        ClueDto reduceClueDtoOne = new ClueDto();
        reduceClueDtoOne.setNowTotal(0);
        //第二种形态
        ClueDto reduceClueDtoTwo = new ClueDto();
        reduceClueDtoTwo.setNowTotal(0);
        //第三种形态
        ClueDto reduceClueDtoThree = new ClueDto();
        reduceClueDtoThree.setNowTotal(0);
        //第四种形态
        ClueDto reduceClueDtoFour = new ClueDto();
        reduceClueDtoFour.setNowTotal(0);
        if(nowList.size()>0){
            for(int i=0;i<nowList.size();i++){
                if("第一种形态".equals(nowList.get(i).getName())){
                    clueDtoOne.setNowTotal(nowList.get(i).getValue().intValue());
                    continue;
                }
                if("第二种形态".equals(nowList.get(i).getName())){
                    clueDtoTwo.setNowTotal(nowList.get(i).getValue().intValue());
                    continue;
                }
                if("第三种形态".equals(nowList.get(i).getName())){
                    clueDtoThree.setNowTotal(nowList.get(i).getValue().intValue());
                    continue;
                }
                if("第四种形态".equals(nowList.get(i).getName())){
                    clueDtoFour.setNowTotal(nowList.get(i).getValue().intValue());
                    continue;
                }
            }
        }else{
            clueDtoOne.setNowTotal(0);
            clueDtoTwo.setNowTotal(0);
            clueDtoThree.setNowTotal(0);
            clueDtoFour.setNowTotal(0);
        }
        if(reduceList.size()>0){
            for(int i=0;i<reduceList.size();i++){
                if("第一种形态".equals(reduceList.get(i).getName())){
                    reduceClueDtoOne.setNowTotal(reduceList.get(i).getValue().intValue());
                    continue;
                }
                if("第二种形态".equals(reduceList.get(i).getName())){
                    reduceClueDtoTwo.setNowTotal(reduceList.get(i).getValue().intValue());
                    continue;
                }
                if("第三种形态".equals(reduceList.get(i).getName())){
                    reduceClueDtoThree.setNowTotal(reduceList.get(i).getValue().intValue());
                    continue;
                }
                if("第四种形态".equals(reduceList.get(i).getName())){
                    reduceClueDtoFour.setNowTotal(reduceList.get(i).getValue().intValue());
                    continue;
                }
            }
        }else{
            reduceClueDtoOne.setNowTotal(0);
            reduceClueDtoTwo.setNowTotal(0);
            reduceClueDtoThree.setNowTotal(0);
            reduceClueDtoFour.setNowTotal(0);
        }
        clueDtoOne.setList(clueDtoOneList);
        clueDtoTwo.setList(clueDtoTwoList);
        clueDtoThree.setList(clueDtoThreeList);
        clueDtoFour.setList(clueDtoFourList);
clueDtoOne.setReduceTotal(XMathUtil.reducePercent(clueDtoOne.getNowTotal(),reduceClueDtoOne.getNowTotal()));
        clueDtoTwo.setReduceTotal(XMathUtil.reducePercent(clueDtoTwo.getNowTotal(),reduceClueDtoTwo.getNowTotal()));
        clueDtoThree.setReduceTotal(XMathUtil.reducePercent(clueDtoThree.getNowTotal(),reduceClueDtoThree.getNowTotal()));
        clueDtoFour.setReduceTotal(XMathUtil.reducePercent(clueDtoFour.getNowTotal(),reduceClueDtoFour.getNowTotal()));

        fourFromsDto.setFastFroms(clueDtoOne);
        fourFromsDto.setTwoFroms(clueDtoTwo);
        fourFromsDto.setThreeFroms(clueDtoThree);
        fourFromsDto.setFourFroms(clueDtoFour);
        return ResultGenerator.genSuccessResult(fourFromsDto);
    }
    @ApiOperation(value = "initFormsTotal")
    @PostMapping("/initFormsTotal")
    /**
     * @Description:  个人影响期至数据等初始化
     * @param []
     * @return com.anshare.project.core.ResultCore.Result
     * @throws
     * @author WKF
     * @UpdateUser:
     * @UpdateDate:   2019/1/18 9:46
     * @UpdateRemark:   修改内容
     * @date  2019/1/18 9:46
     */
public Columns setFormatter(Formatter formatter) {

            this.formatter = formatter;
            return this;
        }
        /**
         * 宽度获得
         * @return 宽度
         */
        public int getWidth() {

            return width;
        }

        /** 
         * 设置表格宽度（字符为单位） 
         * @param width 宽
         * @return 列信息
         */
        public Columns setWidth(int width) {

            this.width = width;
            return this;
        }
    }
    public Result initFormsTotal() {
        List<Person> p = personService.findWithIds();
        for(int i=0;i<p.size();i++){
            if(org.apache.commons.lang3.StringUtils.isNotEmpty(p.get(i).getId())){
                Person person = personService.findById(p.get(i).getId());
                if(person!=null){
                    //计算出处理方式,处理方式,处理类别(按人，然后按形态，最后按分数倒序排序查询)
                    DocApplication d = new DocApplication();
                    d.setPersonid(p.get(i).getId());
                    List<DocApplication> list = docApplicationService.formsPoint(d);
                    if(list.size()>0){
                        person.setProcesstype(list.get(0).getProcessmethoddict());//处理方式
                        person.setProcesstypedict(list.get(0).getProcesstypedict());//处理类别
                        person.setProcesstime(list.get(0).getProcesstime());//处理时间
                        person.setFourforms(new Double(list.get(0).getRemark()));//最高四种形态扣分数
                    }else{
                        person.setFourforms((double)0);
                    }
                    //计算最长影响期至
                    List<DocApplication> influenceList = docApplicationService.formsInfluence(d);
                    if(influenceList.size()>0){
                        person.setInfluencetime(influenceList.get(0).getInfluencetime());//影响期至
                        person.setIsinfluence(influenceList.get(0).getIsinfluence());//是否影响
                    }else{
                        person.setIsinfluence("否");
                    }
                    personService.update(person);
                }
            }
        }
        return ResultGenerator.genSuccessResult("成功");
    }
}
/**
* @ClassName DocBasicinfo
* @Description 领导干部基础信息实体类
* @Author WKF
* @Date 2018/11/12 14:43
* @UpdateUser:  
* @UpdateDate:     2018/11/12 14:43
* @UpdateRemark:   修改内容
* @Version 1.0
**/
@Data
@Table(name = "doc_basicinfo")
public class DocBasicinfo {
    /**
     * PK
     */
    @Id
    @Column(name = "ID")
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private String id;

    /**
     * 录入人员ID
     */
    @Column(name = "HandleStaff")
    private String handlestaff;

    /**
     * 录入人员所在部门ID
     */
    @Column(name = "HandleDept")
    private String handledept;

    /**
     * 个人关联ID
     */
    @Column(name = "PersonID")
    private String personid;

    /**
     * 档案时间
     */
    @Column(name = "Year")
    private String year;

    /**
     * 姓名
     */
    @Column(name = "Name")
    private String name;

    /**
     * 性别ID
     */
    @Column(name = "Gender")
    private String gender;

    /**
     * 性别名称
     */
    @Transient
    private String genderdict;

    /**
     * 出生年月
     */
    @Column(name = "BirthDate")
    private String birthdate;

    /**
     * 曾用名
     */
    @Column(name = "OldName")
    private String oldname;

    /**
     * 民族ID
     */
    @Column(name = "Nation")
    private String nation;

    /**
     * 民族名称
     */
    @Transient
    private String nationdict;

    /**
     * 身份证号
     */
    @Column(name = "IdCard")
    private String idcard;

    /**
     * 籍贯
     */
    @Column(name = "NativePlace")
    private String nativeplace;

    /**
     * 婚姻状况ID
     */
    @Column(name = "Marriage")
    private String marriage;

    /**
     * 婚姻状况名称
     */
    @Transient
    private String marriagedict;

    /**
     * 文化程度ID
     */
    @Column(name = "HighestEducation")
    private String highesteducation;

    /**
     * 文化程度名称
     */
    @Transient
    private String highesteducationdict;

    /**
     * 政治面貌ID
     */
    @Column(name = "PoliticsStatus")
    private String politicsstatus;

    /**
     * 政治面貌名称
     */
    @Transient
    private String politicsstatusdict;

    /**
     * 入党时间
     */
    @Column(name = "PartyDate")
    private String partydate;

    /**
     * 参加工作时间
     */
    @Column(name = "WorkTime")
    private String worktime;

    /**
     * 所在单位
     */
    @Column(name = "WorkUnit")
    private String workunit;

    /**
     * 单位性质ID
     */
    @Column(name = "UnitProperties")
    private String unitproperties;

    /**
     * 单位性质名称
     */
    @Transient
    private String unitpropertiesdict;

    /**
     * 职务ID
     */
    @Column(name = "WorkDuty")
    private String workduty;

    /**
     * 职务级别ID
     */
    @Column(name = "WorkLevel")
    private String worklevel;

    /**
     * 职务级别名称
     */
    @Transient
    private String workleveldict;

    /**
     * 职称ID
     */
    @Column(name = "WorkLevelName")
    private String worklevelname;

    /**
     * 职称名称
     */
    @Transient
    private String worklevelnamedict;

    /**
     * 分管工作
     */
    @Column(name = "WorkCharge")
    private String workcharge;

    /**
     * 任现职时间
     */
    @Column(name = "DutyTime")
    private String dutytime;

    /**
     * 家庭住址
     */
    @Column(name = "Address")
    private String address;

    /**
     * 邮编
     */
    @Column(name = "PostalCode")
    private String postalcode;

    /**
     * 全日制学历
     */
    @Column(name = "Education")
    private String education;

    /**
     * (全日制)毕业院校系及专业
     */
    @Column(name = "FullSchoolMajor")
    private String fullschoolmajor;

    /**
     * 在职教育
     */
    @Column(name = "JobEducation")
    private String jobeducation;

    /**
     * 在职)毕业院校系及专业
     */
    @Column(name = "JobSchoolMajor")
    private String jobschoolmajor;

    /**
     * 照片
     */
    @Lob
    @Column(name = "Photo")
    private String photo;

    /**
     * 是否删除
     */
    @Column(name = "IsDeleted")
    private Boolean isdeleted;

    /**
     * 创建时间
     */
    @Column(name = "Timestamp")
    private Date timestamp;

    /**
     *
     */
    @Transient
    private String religion;

    /**
     * 人员状态 [0]-在职;[1]-辞职;[2]-退休;[3]-开除
     */
    @Column(name = "StatePersonnel")
    private String statepersonnel;

    /**
     * 人员状态 [0]-在职;[1]-辞职;[2]-退休;[3]-开除
     */
    @Column(name = "StatePersonnelDict")
    private String statepersonneldict;
}
 /** 
         * 设置数据 
         * @param field 字段 
         * @param title 标题   
         */
        public Columns(String field, String title) {

            this.field = field;
            this.title = title;
        }

        /**
         * 域获得
         * @return 域
         */
        public String getField() {

            return field;
        }

        /**
         * 标题获得
         * @return 标题
         */
        public String getTitle() {

            return title;
        }

        /**
         * 格式获得
         * @return 格式
         */
        public Formatter getFormatter() {

            return formatter;
        }

        /** 
         * 数据处理回调方法 
         * @param formatter  Formatter
         * @return 列信息
         */
/**
* 领导基本管理Controller
*/
@RestController
@RequestMapping("/doc/basicinfo")
@Api(value = "领导基本管理", description = "领导基本管理")
public class DocBasicinfoController {
    @Resource
    private DocBasicinfoService docBasicinfoService;

    @Resource
    private DocResumeService docResumeService;

    @Resource
    private DocEmploymentService docEmploymentService;

    @Resource
    private DocRelationinfoService docRelationinfoService;

    @Resource
    private DocAnnualcheckService docAnnualcheckService;

    @Resource
    private DocExtracontentService docExtracontentService;

    @Resource
    private DocCompanyService docCompanyService;

    @Resource
    private DocIncomeService docIncomeService;

    @Resource
    private DocExtraincomeService docExtraincomeService;

    @Resource
    private DocHouseService docHouseService;

    @Resource
    private DocStockService docStockService;

    @Resource
    private DocFundService docFundService;

    @Resource
    private DocInsuranceService docInsuranceService;

    @Resource
    private DocOverseaaccountService docOverseaaccountService;

    @Resource
    private DocOverseainvestmentService docOverseainvestmentService;

    @Resource
    private DocFinancialLeaseService docFinancialLeaseService;

    @Resource
    private DocDebtsService docDebtsService;

    @Resource
    private DocCarService docCarService;

    @Resource
    private DocPassportService docPassportService;

    @Resource
    private DocGoAbroadService docGoAbroadService;

    @Resource
    private DocToAbroadService docToAbroadService;

    @Resource
    private DocStudyAbroadService docStudyAbroadService;

    @Resource
    private DocImmigrationService docImmigrationService;

    @Resource
    private DocLivingaboardService docLivingaboardService;

    @Resource
    private DocMarriageService docMarriageService;

    @Resource
    private DocOverseamarriageService docOverseamarriageService;

    @Resource
    private DocSpecialmarriageService docSpecialmarriageService;

    @Resource
    private DocPartyStyleService docPartyStyleService;

    @Resource
    private DocApplicationService docApplicationService;

    @Resource
    private DocAwardService docAwardService;

    @Resource
    private DocHandService docHandService;

    @Resource
    private DocHandMattersService docHandMattersService;

    @Resource
    private DocInstitutionService docInstitutionService;

    @Resource
    private DocAuditService docAuditService;

    @Resource
    private DocDemocraticService docDemocraticService;

    @Resource
    private DocInvestigationService docInvestigationService;

    @Resource
    private DocCrimeService docCrimeService;

    @Resource
    private DocStipulateService docStipulateService;

    @Resource
    private PersonService personService;

    @Resource
    private DynamictablesService dynamictablesService;

    @Resource
    private FormService formService;

    @Resource
    private AffixService affixService;

    @Resource
    private AdCodelistService adCodelistService;

    public static String fileWordTemp;
    @Value("${file.wordtemp}")
    public void setFileWordTemp(String fileWordTemp) {
        DocBasicinfoController.fileWordTemp = fileWordTemp;
    }
    @ApiOperation(value = "新增领导基本情况")
    @PostMapping(value = "/add",produces = "application/json;charset=UTF-8")
    @MyLog(value = "添加记录")  //这里添加了AOP的自定义注解
    public Result add(@RequestBody(required = true) DocBasicinfo docBasicinfo) {
        Person person = personService.findById(docBasicinfo.getPersonid());
        Boolean b = StringUtils.isEmpty(docBasicinfo.getId());
        docBasicinfoService.save(docBasicinfo, b);
        //更新用户的头像
        if(StringUtils.isNotEmpty(docBasicinfo.getPhoto())){
            person.setPhoto(docBasicinfo.getPhoto());
        }
        personService.update(getOnePerson(person,docBasicinfo));
        return ResultGenerator.genSuccessResult(docBasicinfo.getId(),"保存成功");
    }
    @ApiOperation(value = "deleteDocBasicinfo")
    @PostMapping("/delete")
    @MyLog(value = "删除记录")  //这里添加了AOP的自定义注解
    public Result delete(@RequestParam String id) {
        docBasicinfoService.deleteById(id);
        return ResultGenerator.genSuccessResult("删除成功");
    }
    @ApiOperation(value = "updateDocBasicinfo")
    @MyLog(value = "更新记录")  //这里添加了AOP的自定义注解
    @PostMapping(value = "/update",produces = "application/json;charset=UTF-8")
    public Result update(@RequestBody DocBasicinfo docBasicinfo) {
        docBasicinfoService.update(docBasicinfo);
        //更新用户头像
        Person person = personService.findById(docBasicinfo.getPersonid());
        if(StringUtils.isNotEmpty(docBasicinfo.getPhoto()) && !(docBasicinfo.getPhoto()).equals(person.getPhoto())){
            person.setPhoto(docBasicinfo.getPhoto());
        }
        personService.update(getOnePerson(person,docBasicinfo));
        return ResultGenerator.genSuccessResult("更新成功");
    }

    public Person getOnePerson(Person person,DocBasicinfo docBasicinfo){
        if(StringUtils.isNotEmpty(docBasicinfo.getWorkunit())){//工作单位
            person.setWorkunit(docBasicinfo.getWorkunit());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getWorkduty())){//职务
            person.setWorkduty(docBasicinfo.getWorkduty());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getWorklevel())){//职级ID
            person.setWorklevel(docBasicinfo.getWorklevel());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getWorkleveldict())){//职级名称
            person.setWorkleveldict(docBasicinfo.getWorkleveldict());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getWorkcharge())){//分管工作
            person.setWorkcharge(docBasicinfo.getWorkcharge());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getDutytime())){//任现职时间
            person.setDutynowtime(docBasicinfo.getDutytime());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getGender())){//性别ID
            person.setGender(docBasicinfo.getGender());
            person.setGenderdict(getCodeValue(docBasicinfo.getGender(),ProjectConstant.XB));//性别名称
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getNation())){//民族ID
            person.setNation(docBasicinfo.getNation());
            person.setNationdict(getCodeValue(docBasicinfo.getNation(),ProjectConstant.MZ));//民族名称
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getBirthdate())){//出生年月
            person.setBirthdate(docBasicinfo.getBirthdate());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getMarriage())){//婚姻ID
            person.setMarriage(docBasicinfo.getMarriage());
            person.setMarriagedict(getCodeValue(docBasicinfo.getMarriage(),ProjectConstant.HYXZ));//婚姻名称
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getHighesteducation())){//文化程度ID
            person.setHighesteducation(docBasicinfo.getHighesteducation());
            person.setHighesteducationdict(getCodeValue(docBasicinfo.getHighesteducation(),ProjectConstant.WHCD));//文化程度名称
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getPoliticsstatus())){//政治面貌ID
            person.setPoliticsstatus(docBasicinfo.getPoliticsstatus());
            person.setPoliticsstatusdict(getCodeValue(docBasicinfo.getPoliticsstatus(),ProjectConstant.ZZMM));//政治面貌名称
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getPartydate())){//入党时间
            person.setPartydate(docBasicinfo.getPartydate());
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getUnitproperties())){//单位性质ID
            person.setUnitproperties(docBasicinfo.getUnitproperties());
            person.setUnitpropertiesdict(getCodeValue(docBasicinfo.getUnitproperties(),ProjectConstant.DWXZ));//单位性质名称
        }
        if(StringUtils.isNotEmpty(docBasicinfo.getStatepersonnel())){//人员状态ID
            person.setStatepersonnel(docBasicinfo.getStatepersonnel());
            person.setStatepersonneldict(getCodeValue(docBasicinfo.getStatepersonnel(),ProjectConstant.RYZT));//人员状态
        }
        return person;
    }

    public String getCodeValue(String code,String key){
        String str ="";
        Condition condition = new Condition(AdCodelist.class);
        Example.Criteria criteria  = condition.createCriteria();
        criteria.andEqualTo("codevalue", code);
        criteria.andEqualTo("codetype", key);
        List<AdCodelist> adCodelist = adCodelistService.findByCondition(condition);
        if(adCodelist.size()>0){
            str = adCodelist.get(0).getCodename();
        }
        return str;
    }

    @ApiOperation(value = "领导基本情况详情")
    @PostMapping("/detail")
    public Result detail(@RequestParam String id) {
        DocBasicinfo docBasicinfo = docBasicinfoService.findById(id);
        return ResultGenerator.genSuccessResult(docBasicinfo);
    }

    @ApiOperation(value = "领导基本情况")
    @PostMapping("/list")
    public Result list(@RequestBody SearchConditionsModel searchConditionsModel) {
        PageHelper.startPage(searchConditionsModel.getPageIndex(), searchConditionsModel.getPageSize());
        Condition condition = new Condition(DocBasicinfo.class);
        List<DocBasicinfo> list = docBasicinfoService.findByCondition(ConditionUtils.getConditions(condition,searchConditionsModel.getSearchCondition(),searchConditionsModel.getOrderCondition()));
        PageInfo pageInfo = new PageInfo(list);
        return ResultGenerator.genSuccessResult(pageInfo);
    }
    @ApiOperation(value = "领导基本情况")
    @PostMapping("/list2")
    public Result list2(@RequestBody SearchConditionsModel searchConditionsModel) {
        SqlModel sqlModel =ConditionUtils.getConditionsPlus(searchConditionsModel);
        List<DocBasicinfo> list = docBasicinfoService.list(sqlModel);
        PageInfo pageInfo = new PageInfo(list);
        return ResultGenerator.genSuccessResult(pageInfo);
    }
    /**
     * @Description: 导出
     * @param
     * @return
     * @throws
     * @author WKF
     * @UpdateUser:
     * @UpdateDate:     2018/11/18 19:14
     * @UpdateRemark:   修改内容
     * @date 2018/11/18 19:14
     */
    @ApiOperation(value = "dataExport")
    @PostMapping("/dataExport")
    @MyLog(value = "导出记录")  //这里添加了AOP的自定义注解
    public void dataExport(@RequestBody @Validated ConditionsModel conditionsModel, HttpServletResponse response) {
        PageHelper.startPage(conditionsModel.getPageIndex(), conditionsModel.getPageSize());
        Condition condition = new Condition(DocBasicinfo.class);
        List<DocBasicinfo> list = docBasicinfoService.findByCondition(ConditionUtils.getConditions(condition,conditionsModel.getSearchCondition(),conditionsModel.getOrderCondition()));
        PageInfo pageInfo = new PageInfo(list);
        ExportExcel.exportExcelPlus(conditionsModel.getExportCondition(),pageInfo.getList(),response);
    }
    /**
     * @Description: 简历查询
     * @param
     * @return
     * @throws
     * @author WKF
     * @UpdateUser:
     * @UpdateDate:     2018/11/26 18:01
     * @UpdateRemark:   修改内容
     * @date 2018/11/26 18:01
     */
    @ApiOperation(value = "resume")
    @PostMapping("/resume")
    public Result resume(@RequestBody Person person){
        if(person == null){
            return ResultGenerator.genFailResult("用户不存在");
        }
        String personid = "";
        if(StringUtils.isNotEmpty(person.getIdcard())){
            Person p = personService.findBy("idcard",person.getIdcard());
            personid = p.getId();
        }else{
            personid = person.getId();
        }
        List<Map<String,Object>> list = new ArrayList<>();

        //个人档案（最新数据）
        List<DocBasicinfo> basicinfoList = docBasicinfoService.resumeList(personid);
        if(basicinfoList.size()>0){
            basicinfoList = basicinfoList.subList(0,1);
        }
        list.add(getTablesName("doc_basicinfo",basicinfoList));

        //主要任职情况
        List<DocResume> resumeList = docResumeService.resumeList(personid);
        list.add(getTablesName("doc_resume"+ProjectConstant.RESUME_A,resumeList));

        //兼职情况
        List<DocEmployment> employmentList = docEmploymentService.resumeList(personid);
        list.add(getTablesName("doc_employment"+ProjectConstant.RESUME_A,employmentList));

        //家庭成员主要社会关系
        List<DocRelationinfo> relationinfoList = docRelationinfoService.resumeList(personid);
        list.add(getTablesName("doc_relationinfo"+ProjectConstant.RESUME_A,relationinfoList));

        //年度考核结果
        List<DocAnnualcheck> annualcheckList = docAnnualcheckService.resumeList(personid);
        list.add(getTablesName("doc_annualcheck"+ProjectConstant.RESUME_A,annualcheckList));//5

        //其他需要说明的情况
        List<DocExtracontent> extracontentList = docExtracontentService.resumeList(personid);
        list.add(getTablesName("doc_extracontent"+ProjectConstant.RESUME_A,extracontentList));

        //经商办企业情况
        List<DocCompany> companyList = docCompanyService.resumeList(personid);
        list.add(getTablesName("doc_company"+ProjectConstant.RESUME_A,companyList));

        //领导干部本人工资收入（包括工资及各类奖金、津贴、补贴等）
        List<DocIncome> incomeList = docIncomeService.resumeList(personid);
        list.add(getTablesName("doc_income"+ProjectConstant.RESUME_A,incomeList));

        //领导干部本人工资外收入
        List<DocExtraincome> extraincomeList = docExtraincomeService.resumeList(personid);
        list.add(getTablesName("doc_extraincome"+ProjectConstant.RESUME_A,extraincomeList));

        //领导干部本人、配偶、共同生活的子女为所有人或者共有人的房产情况
        List<DocHouse> docHouseList = docHouseService.resumeList(personid);
        list.add(getTablesName("doc_house"+ProjectConstant.RESUME_A,docHouseList));//10

        //领导干部本人、配偶、共同生活的子女投资或其他方式持有股票的情况
        List<DocStock> docStockList = docStockService.resumeList(personid);
        list.add(getTablesName("doc_stock"+ProjectConstant.RESUME_A,docStockList));

        //领导干部本人、配偶、共同生活的子女投资或其他方式持有基金的情况
        List<DocFund> docFundList = docFundService.resumeList(personid);
        list.add(getTablesName("doc_fund"+ProjectConstant.RESUME_A,docFundList));

        //领导干部本人、配偶、共同生活的子女投资或者以其他方式持有投资型保险的情况
        List<DocInsurance> docInsuranceList = docInsuranceService.resumeList(personid);
        list.add(getTablesName("doc_insurance"+ProjectConstant.RESUME_A,docInsuranceList));

        //领导干部本人、配偶、共同生活的子女在国（境）外的存款情况
        List<DocOverseaaccount> docOverseaaccountList = docOverseaaccountService.resumeList(personid);
        list.add(getTablesName("doc_overseaaccount"+ProjectConstant.RESUME_A,docOverseaaccountList));

        //领导干部本人、配偶、共同生活的子女在国（境）外的投资情况
        List<DocOverseainvestment> docOverseainvestmentList = docOverseainvestmentService.resumeList(personid);
        list.add(getTablesName("doc_overseainvestment"+ProjectConstant.RESUME_A,docOverseainvestmentList));//15

        //领导干部本人、配偶、共同生活的子女为所有人或共有人的财物出租情况
        List<DocFinancialLease> docFinancialLeaseList = docFinancialLeaseService.resumeList(personid);
        list.add(getTablesName("doc_financial_lease"+ProjectConstant.RESUME_A,docFinancialLeaseList));

        //领导干部本人、配偶、共同生活的子女债权债务情况
        List<DocDebts> docDebtsList = docDebtsService.resumeList(personid);
        list.add(getTablesName("doc_debts"+ProjectConstant.RESUME_A,docDebtsList));

        //领导干部本人、配偶、共同生活的子女为所有人或共有人的车辆情况
        List<DocCar> docCarList = docCarService.resumeList(personid);
        list.add(getTablesName("doc_car"+ProjectConstant.RESUME_A,docCarList));

        //领导干部本人持有因私出国（境）证件情况
        List<DocPassport> docPassportList = docPassportService.resumeList(personid);
        list.add(getTablesName("doc_passport"+ProjectConstant.RESUME_A,docPassportList));

        //领导干部本人因公/私出国（境）情况
        List<DocGoAbroad> docGoAbroadList = docGoAbroadService.resumeList(personid,"0");
        list.add(getTablesName("doc_go_abroad"+ProjectConstant.RESUME_A,"0",docGoAbroadList));//20

        //领导干部本人因公/私出国（境）情况
        List<DocGoAbroad> docGoAbroadSelfList = docGoAbroadService.resumeList(personid,"1");
        list.add(getTablesName("doc_go_abroad"+ProjectConstant.RESUME_A,"1",docGoAbroadSelfList));

        //配偶、子女及其配偶因私出国（境）情况
        List<DocToAbroad> docToAbroadList = docToAbroadService.resumeList(personid);
        list.add(getTablesName("doc_to_abroad"+ProjectConstant.RESUME_A,docToAbroadList));

        //配偶、子女及其配偶出国（境）留学情况
        List<DocStudyAbroad> docStudyAbroadList = docStudyAbroadService.resumeList(personid);
        list.add(getTablesName("doc_study_abroad"+ProjectConstant.RESUME_A,docStudyAbroadList));

        //配偶、子女及移居国（境）外情况
        List<DocImmigration> docImmigrationList = docImmigrationService.resumeList(personid);
        list.add(getTablesName("doc_immigration"+ProjectConstant.RESUME_A,docImmigrationList));

        //配偶、子女尚未移居国（境）外，但连续在国（境）外工作、生活一年以上的情况
        List<DocLivingaboard> docLivingaboardList = docLivingaboardService.resumeList(personid);
        list.add(getTablesName("doc_livingaboard"+ProjectConstant.RESUME_A,docLivingaboardList));//25

        //领导干部本人的婚姻情况
        List<DocMarriage> docMarriageList = docMarriageService.resumeList(personid);
        list.add(getTablesName("doc_marriage"+ProjectConstant.RESUME_A,docMarriageList));

        //领导干部子女与外国人、无国籍人通婚情况
        List<DocOverseamarriage> docOverseamarriageList = docOverseamarriageService.resumeList(personid);
        list.add(getTablesName("doc_overseamarriage"+ProjectConstant.RESUME_A,docOverseamarriageList));

        //领导干部子女与港澳以及台湾居民通婚情况
        List<DocSpecialmarriage> docSpecialmarriageList = docSpecialmarriageService.resumeList(personid);
        list.add(getTablesName("doc_specialmarriage"+ProjectConstant.RESUME_A,docSpecialmarriageList));

        //领导干部落实党风廉政建设责任制情况表
        List<DocPartyStyle> docPartyStyleList = docPartyStyleService.resumeList(personid);
        list.add(getTablesName("doc_party_style"+ProjectConstant.RESUME_A,docPartyStyleList));

        //领导干部接受监督执纪“四种形态”运用情况表
        List<DocApplication> docApplicationList = docApplicationService.resumeList(personid);
        list.add(getTablesName("doc_application"+ProjectConstant.RESUME_A,docApplicationList));//30

        //领导干部受表彰、奖励情况表
        List<DocAward> docAwardList = docAwardService.resumeList(personid);
        list.add(getTablesName("doc_award"+ProjectConstant.RESUME_A,docAwardList));

        //领导干部上交礼品情况表
        List<DocHand> docHandGiftList = docHandService.resumeList(personid,"0");
        list.add(getTablesName("doc_hand"+ProjectConstant.RESUME_A,"0",docHandGiftList));

        //领导干部上交礼券情况表
        List<DocHand> docHandTicketList = docHandService.resumeList(personid,"1");
        list.add(getTablesName("doc_hand"+ProjectConstant.RESUME_A,"1",docHandTicketList));

        //领导干部上交礼金情况表
        List<DocHand> docHandCashList = docHandService.resumeList(personid,"2");
        list.add(getTablesName("doc_hand"+ProjectConstant.RESUME_A,"2",docHandCashList));

        //领导干部参与操办本人及近亲属婚丧喜庆事宜情况表
        List<DocHandMatters> docHandMattersList = docHandMattersService.resumeList(personid);
        list.add(getTablesName("doc_hand_matters"+ProjectConstant.RESUME_A,docHandMattersList));//35

        //领导干部执行民主集中制、“三重一大”事项报告、“四个不直接分管”等制度情况表
        List<DocInstitution> docInstitutionList = docInstitutionService.resumeList(personid);
        list.add(getTablesName("doc_institution"+ProjectConstant.RESUME_A,docInstitutionList));

        //领导干部离任审计和任期经济责任审计情况表
        List<DocAudit> docAuditList = docAuditService.resumeList(personid);
        list.add(getTablesName("doc_audit"+ProjectConstant.RESUME_A,docAuditList));

        //领导干部参加民主生活会情况表
        List<DocDemocratic> docDemocraticList = docDemocraticService.resumeList(personid);
        list.add(getTablesName("doc_democratic"+ProjectConstant.RESUME_A,docDemocraticList));

        //领导干部被信访举报调查处理情况表
        List<DocInvestigation> docInvestigationList = docInvestigationService.resumeList(personid);
        list.add(getTablesName("doc_investigation"+ProjectConstant.RESUME_A,docInvestigationList));

        //领导干部配偶、子女及其配偶被司法机关追究刑事责任情况表
        List<DocCrime> docCrimeList = docCrimeService.resumeList(personid);
        list.add(getTablesName("doc_crime"+ProjectConstant.RESUME_A,docCrimeList));//40

        //领导干部执行其他廉洁自律规定情况表
        List<DocStipulate> docStipulateList = docStipulateService.resumeList(personid);
        list.add(getTablesName("doc_stipulate"+ProjectConstant.RESUME_A,docStipulateList));

        //附件
        List<Affix> affixList = affixService.resumeList(personid);
        list.add(getTablesName("affix"+ProjectConstant.RESUME_A,affixList));
        return ResultGenerator.genSuccessResult(list);
    }

    public Map getTablesName(String tablename,List<?> list){
        Map<String,Object> map = new HashMap<>();
        if("doc_basicinfo".equals(tablename)){
            Condition condition = new Condition(Form.class);
            condition.createCriteria()
                    .andEqualTo("tableName", tablename);
            List<Form> form = formService.findByCondition(condition);
            if(form.size()>0){
                map.put("tableColumn",form.get(0));
                map.put("tableValue",list);
            }
        }else{
            Condition condition = new Condition(Dynamictables.class);
            condition.createCriteria()
                    .andEqualTo("tableName", tablename);
            List<Dynamictables> dynamictables = dynamictablesService.findByCondition(condition);
            if(dynamictables.size()>0){
                Dynamictables dynamictables1 = dynamictables.get(0);
                dynamictables1.setTableName(dynamictables1.getTableName());
                map.put("tableColumn",dynamictables1);
                map.put("tableValue",list);
            }
        }
        return map;
    }

    public Map getTablesName(String tablename,String type,List<?> list){
        Map<String,Object> map = new HashMap<>();
        Condition condition = new Condition(Dynamictables.class);
        condition.createCriteria()
                .andEqualTo("tableName", tablename);
        List<Dynamictables> dynamictables = dynamictablesService.findByCondition(condition);
        if(dynamictables.size()>0){
            Dynamictables dynamictables1 = dynamictables.get(0);
            dynamictables1.setTableName(dynamictables1.getTableName() +type);
            map.put("tableColumn",dynamictables1);
            map.put("tableValue",list);
        }
        return map;
    }

    @PostMapping("/exportReport")
    public void exportReport(@RequestBody DocBasicinfo basicinfo, HttpServletRequest request, HttpServletResponse response){
        String personid = basicinfo.getPersonid();
        Map<String,Object> params = new HashMap<>();
        Person person = personService.findById(personid);
        Condition baseInfoCondition = new Condition(DocBasicinfo.class);
        Example.Criteria baseInfoCriteria  = baseInfoCondition.createCriteria();
        baseInfoCriteria.andEqualTo("isdeleted", 0);
        baseInfoCriteria.andEqualTo("personid", personid);
        baseInfoCondition.setOrderByClause(" year  DESC");
        List<DocBasicinfo> list = docBasicinfoService.findByCondition(baseInfoCondition);
        String nowDate = DateUtil.getDate();
        String name = person.getName();
        if(list.size()>0){
            DocBasicinfo docBasicinfo = list.get(0);
            //个人基本信息
            String personInfo =name;
            //现任职情况
            String resumeInfo ="";
            //主要任职情况
            String resumeAllInfo = "";
            String personAllInfo = "";
            //家庭成员及主要社会关系
            String relationInfo ="";
            //被信访举报调查处理情况
            String investigationInfo = "";
            //接受监督执纪“四种形态”运用情况
            String applicationInfo = "";
@ApiOperation(value = "initFormsTotal")
    @PostMapping("/initFormsTotal")
    /**
     * @Description:  个人影响期至数据等初始化
     * @param []
     * @return com.anshare.project.core.ResultCore.Result
     * @throws
     * @author WKF
     * @UpdateUser:
     * @UpdateDate:   2019/1/18 9:46
     * @UpdateRemark:   修改内容
     * @date  2019/1/18 9:46
     */
    public Result initFormsTotal() {
        List<Person> p = personService.findWithIds();
        for(int i=0;i<p.size();i++){
            if(org.apache.commons.lang3.StringUtils.isNotEmpty(p.get(i).getId())){
                Person person = personService.findById(p.get(i).getId());
                if(person!=null){
                    //计算出处理方式,处理方式,处理类别(按人，然后按形态，最后按分数倒序排序查询)
                    DocApplication d = new DocApplication();
                    d.setPersonid(p.get(i).getId());
                    List<DocApplication> list = docApplicationService.formsPoint(d);
                    if(list.size()>0){
                        person.setProcesstype(list.get(0).getProcessmethoddict());//处理方式
                        person.setProcesstypedict(list.get(0).getProcesstypedict());//处理类别
                        person.setProcesstime(list.get(0).getProcesstime());//处理时间
                        person.setFourforms(new Double(list.get(0).getRemark()));//最高四种形态扣分数
                    }else{
                        person.setFourforms((double)0);
                    }
                    //计算最长影响期至
                    List<DocApplication> influenceList = docApplicationService.formsInfluence(d);
                    if(influenceList.size()>0){
                        person.setInfluencetime(influenceList.get(0).getInfluencetime());//影响期至
                        person.setIsinfluence(influenceList.get(0).getIsinfluence());//是否影响
                    }else{
                        person.setIsinfluence("否");
                    }
                    personService.update(person);
                }
            }
        }
        return ResultGenerator.genSuccessResult("成功");
    }
}

/**
* @ClassName DocBasicinfo
* @Description 领导干部基础信息实体类
* @Author WKF
* @Date 2018/11/12 14:43
* @UpdateUser:  
* @UpdateDate:     2018/11/12 14:43
* @UpdateRemark:   修改内容
* @Version 1.0
**/
@Data
@Table(name = "doc_basicinfo")
public class DocBasicinfo {
    /**
     * PK
     */
    @Id
    @Column(name = "ID")
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private String id;

    /**
     * 录入人员ID
     */
    @Column(name = "HandleStaff")
    private String handlestaff;

    /**
     * 录入人员所在部门ID
     */
    @Column(name = "HandleDept")
    private String handledept;

    /**
     * 个人关联ID
     */
    @Column(name = "PersonID")
    private String personid;

    /**
     * 档案时间
     */
    @Column(name = "Year")
    private String year;

    /**
     * 姓名
     */
    @Column(name = "Name")
    private String name;

    /**
     * 性别ID
     */
    @Column(name = "Gender")
    private String gender;

    /**
     * 性别名称
     */
    @Transient
    private String genderdict;

    /**
     * 出生年月
     */
    @Column(name = "BirthDate")
    private String birthdate;

    /**
     * 曾用名
     */
    @Column(name = "OldName")
    private String oldname;

    /**
     * 民族ID
     */
    @Column(name = "Nation")
    private String nation;

    /**
     * 民族名称
     */
    @Transient
    private String nationdict;

    /**
     * 身份证号
     */
    @Column(name = "IdCard")
    private String idcard;

    /**
     * 籍贯
     */
    @Column(name = "NativePlace")
    private String nativeplace;

    /**
     * 婚姻状况ID
     */
    @Column(name = "Marriage")
    private String marriage;

    /**
     * 婚姻状况名称
     */
    @Transient
    private String marriagedict;

    /**
     * 文化程度ID
     */
    @Column(name = "HighestEducation")
    private String highesteducation;

    /**
     * 文化程度名称
     */
    @Transient
    private String highesteducationdict;

    /**
     * 政治面貌ID
     */
    @Column(name = "PoliticsStatus")
    private String politicsstatus;

    /**
     * 政治面貌名称
     */
    @Transient
    private String politicsstatusdict;

    /**
     * 入党时间
     */
    @Column(name = "PartyDate")
    private String partydate;

    /**
     * 参加工作时间
     */
    @Column(name = "WorkTime")
    private String worktime;

    /**
     * 所在单位
     */
    @Column(name = "WorkUnit")
    private String workunit;

    /**
     * 单位性质ID
     */
    @Column(name = "UnitProperties")
    private String unitproperties;

    /**
     * 单位性质名称
     */
    @Transient
    private String unitpropertiesdict;

    /**
     * 职务ID
     */
    @Column(name = "WorkDuty")
    private String workduty;

    /**
     * 职务级别ID
     */
    @Column(name = "WorkLevel")
    private String worklevel;

    /**
     * 职务级别名称
     */
    @Transient
    private String workleveldict;
    /**
     * 职称ID
     */
    @Column(name = "WorkLevelName")
    private String worklevelname
    /**
     * 职称名称
     */
    @Transient
    private String worklevelnamedict;
    /**
     * 分管工作
     */
    @Column(name = "WorkCharge")
    private String workcharge;

    /**
     * 任现职时间
     */
    @Column(name = "DutyTime")
    private String dutytime;

    /**
     * 家庭住址
     */
    @Column(name = "Address")
    private String address;
    /**
     * 邮编
     */
    @Column(name = "PostalCode")
    private String postalcode;
    /**
     * 全日制学历
     */
    @Column(name = "Education")
    private String education;
    /**
     * (全日制)毕业院校系及专业
     */
    @Column(name = "FullSchoolMajor")
    private String fullschoolmajor;

    /**
     * 在职教育
     */
    @Column(name = "JobEducation")
    private String jobeducation;

    /**
     * 在职)毕业院校系及专业
     */
    @Column(name = "JobSchoolMajor")
    private String jobschoolmajor;

    /**
     * 照片
     */
    @Lob
    @Column(name = "Photo")
    private String photo;

    /**
     * 是否删除
     */
    @Column(name = "IsDeleted")
    private Boolean isdeleted;

    /**
     * 创建时间
     */
    @Column(name = "Timestamp")
    private Date timestamp;

    /**
     *
     */
    @Transient
    private String religion;

    /**
     * 人员状态 [0]-在职;[1]-辞职;[2]-退休;[3]-开除
     */
    @Column(name = "StatePersonnel")
    private String statepersonnel;

    /**
     * 人员状态 [0]-在职;[1]-辞职;[2]-退休;[3]-开除
     */
    @Column(name = "StatePersonnelDict")
    private String statepersonneldict;
}
            